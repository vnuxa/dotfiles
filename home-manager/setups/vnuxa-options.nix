{ config, ... }:
{
#INFO: See configuration_variables.nix for all the available configuration options and descriptions
    config = {
        theme = {
            wallpaper = "night.jpg";

            hyprland = {
                dot-name = "aesthetic";
            };
        };
    };
    
}
